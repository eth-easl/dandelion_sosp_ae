---

# TODO [nku] could add default skeleton for super etl

# Checkout the example from the demo_project: demo_project/doe-suite-config/super_etl/demo_plots.yml

$SUITE_ID$:
  load_latency_matmul: 1753200739
  # mixed_workload_sosp: 1744542177
  # mixed_workload_sosp_firecracker: 1744729514

$ETL$:
  matmul_128_97hot:
    experiments:
      load_latency_matmul: "*"
    extractors:
      LatencyExtractor: {}
      IgnoreExtractor: 
        file_regex: '.*\.[log|pkl]'
    transformers: []
    loaders:
      MatmulMixedLoadLatencyPlotLoader:
        percentiles: [50, 99]
  # mixed_workload:
  #   experiments:
  #     mixed_workload_sosp: "*"
  #     mixed_workload_sosp_firecracker: "*"
  #   extractors:
  #     MixedWorkloadExtractor: {}
  #     IgnoreExtractor:
  #       file_regex: '.*\.[log|pkl]'
  #   transformers: []
  #   loaders:
  #     MixedWorkloadLoader: {}
